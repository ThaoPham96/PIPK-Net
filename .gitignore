# Python
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
build/
dist/
.eggs/
*.so

# Virtual environments
.venv/
venv/
env/
.conda/

# Jupyter
.ipynb_checkpoints/

# Tutorial / run-time artefacts (regenerated by running the code)
# my_predictions.csv is written by the tutorial batch cell (matches any working dir)
my_predictions.csv
# root-level default output of `pipknet predict`
# (anchored with a leading slash so checkpoints/*/fold_*/predictions.csv stay tracked)
/predictions.csv
# one-off generator scripts and temporary executed-notebook copies
_gen_*.py
_*.ipynb

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# NOTE: data/, checkpoints/, outputs/, and examples/tutorial.ipynb ARE committed --
# they are needed to reproduce (and display) the manuscript figures and tables.
