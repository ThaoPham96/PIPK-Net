"""Figure modules -- one per manuscript figure (group).

Each module exposes a ``make()`` function that reads the frozen predictions and
writes the figure to ``outputs/figures/`` with the manuscript's basename.
"""
